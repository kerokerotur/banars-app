# web

banars の Web クライアントです。

## セットアップ

1. `cd apps/web`
2. `npm install`
3. `npm run dev`

## 環境変数

Vite の環境変数は `VITE_` プレフィックスで管理します。

例:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
- `VITE_LINE_LIFF_ID`
